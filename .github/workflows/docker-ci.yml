name: Flask Docker CI/CD

on:
  push:
    branches:
      - main

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ADD THIS DEBUG STEP
      - name: Debug Secret Lengths
        run: |
          echo "Username length is: ${{ length(secrets.DOCKER_USERNAME) }}"
          echo "Token length is: ${{ length(secrets.DOCKER_TOKEN) }}"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # ... keep the rest of your file exactly the same ...